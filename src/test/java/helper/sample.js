function fn() {
	var schema = {
		employeeSchema: {
			id: '#number',
			firstName: '#string',
			lastName: '#string',
			dob: '#string',
			fullName: '#string',
			age: '#string'
		}
	}
	return schema;
}